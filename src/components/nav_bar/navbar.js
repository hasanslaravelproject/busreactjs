import { AppBar, Toolbar } from "@material-ui/core"
import React from "react"
export const navbar=()=>{
    //routes to add , all in the side panel ya
    return(
        <>
        <AppBar position="static">
            <Toolbar style={{  }}>navbar</Toolbar>
        </AppBar>
        
        </>
    )
}